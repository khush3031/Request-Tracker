# Publishing is handled exclusively by the GitHub Actions CI/CD pipeline.
# Local `npm publish` is blocked by the prepublishOnly script in package.json.
# Auth is configured at runtime by actions/setup-node — do not set _authToken here.
