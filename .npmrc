# Publishing is handled exclusively by the GitHub Actions CI/CD pipeline.
# Running `npm publish` locally will fail without the NPM_TOKEN env variable,
# which is only available inside the GitHub Actions environment.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
